//! Capture abstraction (NC0 M2/M4): a `CaptureBackend` produces `Frame`s
//! from the game window in memory — Windows Graphics Capture preferred,
//! DXGI desktop duplication as fallback. Placeholder until then; nothing
//! here writes files or touches other processes.
