//! Perception stack (NC0 M4/M6): `Detector` / `Matcher` / `OCR` traits and
//! mock implementations only. Real inference arrives with NC1 (ONNX), OCR
//! with NC2. No model downloads, no GPU code in this crate.
