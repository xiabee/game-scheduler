# build output
/bin/
bin/
dist/
*.exe
.gocache/
.gopath/
.gotelemetry/

.nightly/
_debug/

# runtime data (db, logs, screenshots)
/data/
data/
/data_test/
logs/
screenshots/
*.db
*.db-shm
*.db-wal
*.log
tmp/
config.json
config.local.*
.env

# editor/OS
.idea/
.vscode/
*.swp
.DS_Store

# local launch/preview config & scratch logs
.claude/
stest.*
run.log
run.err

# rust controller (NC0)
controller/target/

# NC1: model weights stay external (ROADMAP §4); only manifests are committed
*.onnx
# exception: the tiny hand-built ONNX test fixture (185 bytes, no weights)
!controller/tests/fixtures/*.onnx
# NC1 training scaffolds: never commit training runs or dataset images
runs/
datasets/images/
datasets/labels/
datasets/raw/

# python bytecode (tools/vision scaffolds)
__pycache__/
